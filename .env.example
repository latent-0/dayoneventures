# ---------------------------------------------------------------------------
# Contact-form lead delivery
# Copy this file to `.env` for local dev. On Vercel, set this as an
# Environment Variable in the project settings (Production + Preview).
# ---------------------------------------------------------------------------

# Google Apps Script web-app URL. Deploy apps-script/Code.gs as a Web App
# (see that file for steps) and paste its /exec URL here. Every submission
# is appended as a row to the lead spreadsheet and emailed as a notification.
LEAD_SHEET_URL=
